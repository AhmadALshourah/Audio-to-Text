# خطة تطوير المشروع: Audio-to-Text SaaS

> **الوضع الحالي:** سكريبت Python واحد (`main.py`) يستخدم OpenAI Whisper API لتحويل ملف صوتي محدد يدويًا إلى نص. لا يوجد Frontend، لا يوجد Backend حقيقي، لا يوجد Auth، لا توجد قاعدة بيانات، لا يوجد نظام دفع.
>
> **الهدف:** تحويل المشروع إلى منتج SaaS حقيقي جاهز لاستقبال مستخدمين فعليين، مع صفحة تسويقية (Promo Page)، تسجيل دخول/اشتراك، Dashboard لرفع الملفات الصوتية وتحويلها لنص، نظام اشتراكات/حدود استخدام، وبنية تحتية قابلة للتوسّع.

## القرارات التقنية المعتمدة

> **تحوّل معماري كبير (بعد المرحلة 8):** بقرار المالك، أُلغيت كل الخدمات الخارجية
> (Supabase, Clerk, Upstash) واستُبدلت ببدائل **مستقلة بالكامل** — المشروع الآن
> "استنسخه وشغّله محليًا بدون أي حساب خارجي". الخدمة الخارجية الوحيدة المتبقية هي
> **OpenAI Whisper** (جوهر المنتج، لا بديل مجاني بنفس الجودة).

| القرار | الاختيار الحالي (مستقل) | كان سابقًا | السبب |
|---|---|---|---|
| Frontend | **Next.js (React) + TypeScript** | — | Server Components، SEO |
| Backend | **Next.js API Routes** + **Worker منفصل (يستطلع SQLite)** | Worker + BullMQ | فصل المعالجة الطويلة عن الطلبات |
| Auth | **مبنيّ ذاتيًا** (email+password، scrypt، جلسات بـ cookies آمنة، `node:crypto`) | Clerk | استقلالية كاملة، صفر تبعيات |
| قاعدة البيانات | **SQLite** (ملف واحد) + **Prisma ORM** | Supabase Postgres | لا حساب/سيرفر خارجي، يشتغل offline |
| تخزين الملفات | **نظام ملفات محلي** (`data/uploads/`، يُحذف بعد المعالجة) | Supabase Storage | بساطة، خصوصية (نحتفظ بالنص فقط) |
| قائمة الانتظار | **worker يستطلع جدول SQLite** (claim ذرّي pending→processing) | BullMQ + Upstash Redis | لا Redis، لا خدمة خارجية |
| الدفع/الاشتراكات | **بدون بوابة دفع — Free فقط** | — | دفع حقيقي يتطلب بوابة PCI-compliant؛ المنتج مجاني بالكامل |
| التحويل الصوتي | **OpenAI Whisper** (الخارجي الوحيد المتبقي) | — | جوهر المنتج |
| الاستضافة | **سيرفر Node دائم واحد** (VPS/Railway/Render) أو محليًا | Vercel + Redis منفصل | SQLite + قرص محلي يحتاجان نظام ملفات دائم (مش Vercel serverless) |

---

## نظرة عامة على المراحل (مرتّبة حسب الأهمية)

| # | المرحلة | الأولوية |
|---|---|---|
| 1 | إصلاحات أمنية وبنيوية عاجلة على الكود الحالي | 🔴 حرجة |
| 2 | تصميم البنية المعمارية والـ Monorepo | 🔴 حرجة |
| 3 | قاعدة البيانات ونماذج البيانات (Schema) | 🔴 حرجة |
| 4 | Backend API الأساسي (رفع + تحويل الصوت) | 🔴 حرجة |
| 5 | نظام تسجيل الدخول (Auth: Login / Sign Up) | 🔴 حرجة |
| 6 | معالجة غير متزامنة (Queue + Worker) لدعم الملفات الطويلة | 🟠 عالية |
| 7 | Frontend - لوحة التحكم (Dashboard) الأساسية | 🟠 عالية |
| 8 | صفحة تسويقية (Promo / Landing Page) | 🟠 عالية |
| 9 | ~~نظام الاشتراكات والدفع~~ — ملغاة، Free فقط (انظر التفاصيل) | ✅ محسومة |
| 10 | تخزين الملفات السحابي (Cloud Storage) | 🟡 متوسطة |
| 11 | الأمان المتقدّم وحماية من إساءة الاستخدام | 🟡 متوسطة |
| 12 | المراقبة والتحليلات (Observability) | 🟡 متوسطة |
| 13 | الاختبارات (Testing) | 🟡 متوسطة |
| 14 | CI/CD والنشر | 🟡 متوسطة |
| 15 | الامتثال القانوني (Privacy Policy / ToS / GDPR) | 🟢 منخفضة (لكن مطلوبة قبل الإطلاق الفعلي) |
| 16 | تحسينات ما بعد الإطلاق | 🟢 منخفضة |

---

## المرحلة 1 — إصلاحات عاجلة على الكود الحالي 🔴 ✅ (مُنجزة)

هذه مشاكل موجودة في `main.py` الحالي ويجب حلها فورًا حتى لو بشكل مؤقت قبل إعادة البناء الكامل:

- [ ] إصلاح الخطأ الإملائي `clint` → `client`.
- [ ] إزالة المسار الوهمي `"your mp3 file"` — يجب أن يأتي من مدخل حقيقي (CLI arg أو API request) وليس Hardcoded.
- [ ] استخدام `with open(...)` بدلاً من `open()` بدون إغلاق الملف (تسريب Resource).
- [ ] عدم رفع مفتاح `OPENAI_API_KEY` أو ملف `.env` للـ Git — التأكد من وجود `.gitignore` يشمل `.env` و`.venv`.
- [ ] إضافة معالجة أخطاء أساسية (ملف غير موجود، امتداد غير مدعوم، فشل الاتصال بـ API).
- [ ] إضافة `requirements.txt` أو الانتقال المباشر لبنية Node.js الجديدة.

**ملاحظة:** هذه المرحلة يمكن تنفيذها خلال ساعة، لكنها ضرورية كنقطة انطلاق نظيفة قبل الهدم/البناء.

---

## المرحلة 2 — البنية المعمارية و Monorepo 🔴 ✅ (مُنجزة)

- [ ] إنشاء بنية Monorepo (باستخدام `pnpm workspaces` أو `Turborepo`):
  ```
  /apps
    /web        → Next.js (Frontend + API Routes الخفيفة)
    /worker     → Node/Express + BullMQ (معالجة التحويل الصوتي)
  /packages
    /db         → Prisma schema + Client مشترك
    /shared     → أنواع TypeScript مشتركة (types/DTOs)
  ```
- [ ] إعداد متغيرات البيئة (`.env`) لكل تطبيق على حدة مع `zod` للتحقق من صحتها عند الإقلاع.
- [ ] إعداد ESLint + Prettier + TypeScript strict mode موحّد على مستوى الـ Monorepo.
- [ ] توثيق البنية في `README.md` جديد للمشروع.

---

## المرحلة 3 — قاعدة البيانات ونماذج البيانات 🔴 ✅ (مُنجزة — schema حيّ على Supabase)

تصميم Schema أولي عبر Prisma:

- `User` (مرتبط بـ Clerk عبر `clerkId`)
- `Subscription` (خطة المستخدم: Free / Pro، بدون ربط ببوابة دفع خارجية)
- `Transcription` (ملف صوتي، حالة المعالجة: pending/processing/done/failed، النص الناتج، المدة الزمنية للصوت، التكلفة)
- `UsageLog` (لتتبع الدقائق المستهلكة شهريًا لكل مستخدم لأغراض الفوترة والحدود)

- [ ] كتابة الـ Schema وتوليد الـ Migrations.
- [ ] إعداد Seed script للبيانات التجريبية.
- [ ] تفعيل Row-Level Security إن استُخدم Supabase مباشرة.

---

## المرحلة 4 — Backend API الأساسي 🔴 ✅ (مُنجزة — مُختبرة end-to-end مع Whisper الحقيقي)

- [ ] Endpoint: `POST /api/transcriptions` — استقبال ملف صوتي (أو رابط Signed Upload)، التحقق من الحجم/الامتداد/مدة الصوت.
- [ ] Endpoint: `GET /api/transcriptions/:id` — حالة ونتيجة التحويل.
- [ ] Endpoint: `GET /api/transcriptions` — سجل تحويلات المستخدم.
- [ ] دمج استدعاء Whisper API (نقل منطق `main.py` الحالي إلى خدمة Node منظّمة، أو الإبقاء عليه كـ Python microservice إن أردت إعادة استخدام الكود — **قرار مفتوح، انظر الأسئلة بالأسفل**).
- [ ] التحقق من صحة المدخلات (حجم أقصى، صيغ مدعومة: mp3/wav/m4a...).
- [ ] معالجة الأخطاء بشكل موحّد (Error format ثابت لكل الـ API).

---

## المرحلة 5 — تسجيل الدخول (Auth) 🔴 ✅ (أُعيد بناؤها ذاتيًا — مُختبرة end-to-end)

> بُنيت أولًا بـ Clerk، ثم **أُعيدت بالكامل ذاتيًا** ضمن التحوّل للاستقلالية.
> النسخة الحالية (كلها `node:crypto`، صفر تبعيات خارجية):
- [x] تسجيل/دخول بـ email + password، تجزئة **scrypt** + salt عشوائي.
- [x] جلسات بـ token عشوائي؛ نخزّن **hash الـ token فقط** بالـ DB؛ cookie آمن (httpOnly, sameSite=lax).
- [x] صفحات `/sign-in` و `/sign-up` (نماذج خاصة) + زر خروج.
- [x] حماية المسارات: middleware (فحص وجود الـ cookie) + تحقق حقيقي بـ `requireUserId` (Node).
- [x] المستخدم يُنشأ عند التسجيل (لا حاجة لـ webhook خارجي).
- مُختبر فعليًا: تسجيل → 401 بدون جلسة → رفع → خروج يُبطل الجلسة → دخول بالمستخدم المزروع.

---

## المرحلة 6 — المعالجة غير المتزامنة (Worker) 🟠 ✅ (أُعيد بناؤها — نجاح وفشل مُختبران)

> بُنيت أولًا بـ BullMQ + Upstash Redis، ثم **أُعيدت بلا Redis** ضمن التحوّل للاستقلالية.
- [x] الرفع يُنشئ سجل `pending` ويرجع فورًا (202)؛ لا انتظار لـ Whisper بالطلب.
- [x] Worker منفصل **يستطلع** جدول SQLite ويلتقط المهمة بـ claim ذرّي (`pending → processing`).
- [x] إعادة محاولة بحدّ أقصى (`MAX_TRANSCRIPTION_ATTEMPTS`) قبل `failed` النهائي.
- [x] الواجهة تعمل polling لحالة كل مهمة حتى `done`/`failed`.
- [ ] تقسيم الملفات الطويلة (Chunking) فوق حد Whisper (25MB) — لاحقًا.

---

## المرحلة 7 — Frontend: لوحة التحكم (Dashboard) 🟠 ✅ (مُنجزة)

- [ ] صفحة رفع الملفات (Drag & Drop) مع شريط تقدّم.
- [ ] عرض حالة المعالجة لحظيًا (Processing/Done/Failed).
- [ ] عرض النص الناتج مع خيارات: نسخ، تحميل (TXT/SRT/VTT)، بحث داخل النص.
- [ ] سجل كامل لعمليات التحويل السابقة مع بحث وفلترة.
- [ ] عرض الرصيد المتبقي من الدقائق حسب خطة الاشتراك.
- [ ] تصميم متجاوب (Responsive) لكل الشاشات.

---

## المرحلة 8 — الصفحة التسويقية (Promo / Landing Page) 🟠 ✅ (مُنجزة — شاملة SEO/sitemap/robots)

- [ ] Hero Section: عرض واضح للقيمة المقدّمة (تحويل صوت لنص بدقة عالية وبسرعة).
- [ ] قسم المزايا (Features) مع أيقونات/رسومات.
- [ ] قسم الأسعار (Pricing) يعرض خطط Free/Pro بوضوح.
- [ ] قسم "كيف يعمل" (How it works) بخطوات بسيطة.
- [ ] Testimonials / Social proof (حتى لو placeholder مبدئيًا).
- [ ] CTA واضح يقود لصفحة Sign Up.
- [ ] تحسين SEO أساسي (meta tags, Open Graph, sitemap.xml, robots.txt).
- [ ] سرعة تحميل عالية (Lighthouse score) لأن هذه الصفحة أول تفاعل مع المستخدم المحتمل.

---

## المرحلة 9 — الاشتراكات والدفع ✅ ملغاة (Free فقط إلى أجل غير مسمى)

**القرار النهائي:** لا بوابة دفع خارجية إطلاقًا. معالجة دفعات حقيقية (فيزا/ماستركارد) تتطلب قانونيًا وأمنيًا المرور عبر بوابة مرخّصة (PCI-DSS compliance) — بناء نظام "يسحب من الكرت" ذاتيًا بدون بوابة معتمدة غير آمن وغير قانوني. تم فحص:
- **Stripe:** يتطلب رقم هاتف/حساب بنكي أمريكي للتحقق — عائق حقيقي للمطوّر.
- **Paddle:** Merchant of Record لا يحتاج حضور أمريكي، لكن ما زال خدمة خارجية يحتاجها القرار تفاديها.

**بالنتيجة:** المنتج **مجاني بالكامل** لكل المستخدمين، بدون خطة Pro مدفوعة. ما تم إنجازه فعليًا (يغطي حاجة "حدود الاستخدام" من هذه المرحلة):
- ✅ حد شهري ثابت (30 دقيقة) مفروض عبر `assertQuotaAvailable` (مرحلة 4).
- ✅ عرض الرصيد المتبقي بالـ Dashboard (مرحلة 7).
- ✅ حقول `plan`/`status` بقيت بالـ Schema لمرونة مستقبلية (مثلاً منح خطة `pro` يدويًا لحساب معيّن)، لكن بدون أي مسار شراء فعلي.
- ✅ حُذفت كل الحقول المرتبطة ببوابة دفع خارجية (`stripeCustomerId` وما شابه) من قاعدة البيانات.

---

## المرحلة 10 — تخزين الملفات ✅ (مُنجزة محليًا بدل السحابي)

> بعد التحوّل للاستقلالية: بدل التخزين السحابي (Supabase Storage/S3 + Signed URLs)،
> التخزين الآن **نظام ملفات محلي** — أبسط وأكثر خصوصية ومستقل بالكامل.
- [x] الصوت يُكتب على القرص (`data/uploads/<id>.<ext>`) عند الرفع، والـ worker يقرأه من هناك.
- [x] **حذف فوري** للملف الصوتي بعد المعالجة (نجاح أو فشل نهائي) — نحتفظ بالنص فقط. مُختبر: مجلد الرفع يفضى بعد كل مهمة.
- [ ] (لاحقًا للإنتاج) رفع مباشر من المتصفح لو انتقلنا لتخزين كائنات + تجاوز حد جسم الطلب.

---

## المرحلة 11 — الأمان وحماية من إساءة الاستخدام 🟡 ✅ (مُنجزة — مُختبرة)

- [x] **Rate limiting** in-memory (بلا Redis، يناسب العملية الواحدة): دخول 10/5د/IP، تسجيل 5/ساعة/IP، تحويلات 20/5د/مستخدم — لحماية تكلفة Whisper ومنع brute-force. مُختبر: المحاولة 11 للدخول → 429 + `Retry-After`.
- [x] **حد أقصى للحجم قبل تحميل الملف للذاكرة** — يمنع استنزاف الذاكرة (رفض الحجم الزائد قبل قراءة الـ buffer).
- [x] **فحص Magic bytes** — يتأكد إن المحتوى صوتي فعلاً (WAV/OGG/FLAC/MP4/WebM/MP3). مُختبر: ملف نصي باسم `.mp3` → 400، وWAV حقيقي → 202.
- [x] **Security headers** (X-Content-Type-Options، X-Frame-Options: DENY، Referrer-Policy، Permissions-Policy) + إخفاء `X-Powered-By`. الـ API same-origin فلا حاجة لـ CORS مفتوح.
- [x] **تصليب أمن المصادقة**: تجزئة كلمات السر بـ scrypt، جلسات نخزّن hash التوكن فقط، cookies httpOnly/sameSite، ورسائل خطأ عامة + hash وهمي عند غياب المستخدم (منع تسريب الإيميلات المسجّلة).
- [ ] (لاحقًا للإنتاج) تخزين `OPENAI_API_KEY` في Secret Manager بدل `.env` — المفتاح الخارجي الوحيد المتبقّي.

---

## المرحلة 12 — المراقبة والتحليلات 🟡 ✅ (مُنجزة — logging محلي بدل Sentry/PostHog الخارجيين)

- [x] **Logger منظّم** (`packages/shared/src/logger.ts`) — كل سطر JSON واحد (level/msg/time + حقول حرة)، صفر تبعيات خارجية؛ يطبع لـ stdout/stderr (أي شي يلتقطها لاحقًا: systemd journal، docker logs، أو الطرفية بالتطوير).
- [x] **الـ Worker**: كل مهمة تُسجَّل ببداية/نهاية مع `jobId`, `fileName`, `attempt`, `elapsedMs`, `status`, `audioDurationSeconds`, `costUsd`. مُختبر حيًّا.
- [x] **الـ Web API**: كل طلب يُسجَّل تلقائيًا (method/path/status/elapsedMs) عبر `withErrorHandling` — بدون تعديل أي route handler، لأن Next يمرّر الـ Request الحقيقي دائمًا للـ wrapper بغض النظر عن توقيع الدالة الداخلية. مُختبر حيًّا: `GET /api/usage` بدون auth → `{"level":"warn",...,"status":401}`.
- [x] **تنبيهات محلية بسيطة**: تحذير عند تكلفة مهمة واحدة غير طبيعية (≥ $0.50)، وتحذير عند ارتفاع معدل الفشل (≥50% من آخر 10 مهام) — heuristics داخل عملية الـ worker، بدون خدمة تنبيهات خارجية.
- ~~Sentry/PostHog~~ — غير مناسبين لستاك مستقل بالكامل؛ الـ logging المنظّم البديل كافٍ لحجم المشروع.

---

## المرحلة 13 — الاختبارات 🟡 ✅ (مُنجزة جزئيًا — 57 اختبار، Vitest)

**الأداة: Vitest** (بدل Jest — أسرع، تكامل أفضل مع TypeScript/ESM، تشغيل موحّد عبر turbo).

- [x] **اختبارات وحدة** (بدون قاعدة بيانات، بلا mocking): `packages/shared/src/crypto.test.ts` (7)، `packages/core/src/validation.test.ts` (15 — يشمل فحص magic bytes)، `packages/core/src/errors.test.ts` (7)، `apps/web/src/lib/rate-limit.test.ts` (8 — يشمل fake timers لاختبار انتهاء النافذة الزمنية).
- [x] **اختبارات تكامل** ضد **قاعدة SQLite حقيقية مؤقتة** (`packages/core/test/global-setup.ts` يُنشئ `test.db` منفصل عن `dev.db` عبر `prisma db push`، ويحذفه بعد الانتهاء):
  - `quota.test.ts` (4): حساب الاستهلاك، رفض تجاوز الحصة الشهرية.
  - `auth-service.test.ts` (9): تسجيل، تكرار الإيميل، دخول صحيح/خاطئ، رسالة خطأ موحّدة لإيميل غير موجود (منع التسريب)، صلاحية/إبطال الجلسة.
  - `transcription-service.test.ts` (7): المسار الكامل رفع→claim→معالجة، مع **mock لاستدعاء Whisper** (`vi.mock('./whisper.js')`) لتفادي أي تكلفة/شبكة حقيقية أثناء الاختبار — يتحقق من: إنشاء pending + حفظ الملف على القرص، claim ذرّي لا يكرّر نفس المهمة، نجاح→done+حذف الملف+تسجيل الاستهلاك، فشل مؤقت→إعادة للقائمة، فشل نهائي بعد استنفاد المحاولات→failed+حذف الملف.
  - **المجموع: 57 اختبار ناجح** عبر 3 حزم (`pnpm test` من الجذر عبر turbo).
- [ ] اختبارات E2E (Playwright) للمسارات الحرجة بالمتصفح — مؤجّلة (تحتاج تشغيل متصفح فعلي؛ المسارات الحرجة تحقّقنا منها يدويًا مرارًا خلال الجلسة).
- [ ] اختبار تحميل (Load test) على الـ Worker — مؤجّل، أولوية أقل لمشروع portfolio بحجم مستخدم صغير.

---

## المرحلة 14 — CI/CD والنشر 🟡 ✅ (CI مُنجز ومُختبر — النشر يحتاج قرارك)

- [x] **GitHub Actions** (`.github/workflows/ci.yml`): على كل push/PR لـ `main` — تثبيت (pnpm + cache) → توليد Prisma Client → **lint** → **typecheck** → **test** (57 اختبار) → **build**. مُختبر محليًا بمحاكاة كاملة (بيئة placeholder من `.env.example`، بدون أي أسرار حقيقية) — كل الخطوات نجحت.
- [x] Badge حالة الـ CI بـ `README.md`.

### ⚠️ تصحيح معماري مهم — Vercel/Railway لا يصلحان لهذا الستاك

الخطة الأصلية افترضت Vercel (serverless) + Railway (worker منفصل)، وهذا **لم يعد يعمل** بعد التحوّل للستاك المستقل:
- نظام ملفات Vercel **مؤقت وللقراءة فقط في الإنتاج** — ملف SQLite ما رح يصمد بين الطلبات، وأي كتابة تضيع.
- الـ web والـ worker يشتركان **بنفس القرص** (نفس `dev.db` ونفس `data/uploads/`) — مستحيل بين مزوّدين منفصلين (serverless + سيرفر آخر).

**البديل الصحيح: سيرفر دائم واحد (VPS)** — مثل DigitalOcean Droplet، Hetzner، أو Fly.io (بـ persistent volume) — يشغّل `apps/web` (عبر `next start` أو PM2) و`apps/worker` معًا، على نفس القرص، بعملية واحدة أو اثنتين (process manager مثل `pm2` أو `systemd`).

- [ ] اختيار مزوّد VPS (يحتاج قرارك وحساب — خارج نطاق التنفيذ الحالي).
- [ ] كتابة سكريبت/Dockerfile نشر يشغّل web+worker على نفس القرص (لاحقًا عند اختيار المزوّد).
- [ ] بيئات Development/Production منفصلة (لاحقًا مع النشر الفعلي).

---

## المرحلة 15 — الامتثال القانوني 🟢

- [ ] صفحة Privacy Policy توضح كيفية التعامل مع الملفات الصوتية المرفوعة.
- [ ] صفحة Terms of Service.
- [ ] موافقة صريحة من المستخدم عند الرفع (خاصة إن كانت الملفات تحتوي بيانات حساسة/محادثات).
- [ ] التأكد من امتثال OpenAI نفسه لسياسات البيانات (مراجعة اتفاقية OpenAI API Data Usage).

---

## المرحلة 16 — تحسينات ما بعد الإطلاق 🟢

- [ ] دعم لغات متعددة في الواجهة (i18n) — خاصة العربية والإنجليزية.
- [ ] تصدير بصيغ ترجمة (SRT/VTT) مع ضبط التوقيت (timestamps).
- [ ] دعم تمييز المتحدثين (Speaker diarization) كخاصية Pro.
- [ ] واجهة برمجية (API) عامة للمطورين ليستخدموا الخدمة برمجيًا (خطة B2B).
- [ ] دعم Workspaces/Teams لمشاركة التحويلات بين أعضاء فريق.
- [ ] تطبيق جوّال (React Native) في مرحلة لاحقة إن نجح المنتج.

---

## القرارات النهائية (تم الحسم)

1. **لغة منطق التحويل الصوتي:** إعادة كتابة منطق Whisper بالكامل بـ **Node.js** — لا نُبقي على Python، الـ Stack كله JavaScript/TypeScript موحّد (Next.js + Worker).
2. **الحسابات الخارجية (Clerk/Supabase):** غير جاهزة بعد → أُضيفت **مرحلة 0** أدناه لإنشاء وربط هذه الحسابات قبل البدء بالتطوير الفعلي.
3. **الهوية البصرية:** غير جاهزة → صفحة الـ Promo تبدأ بتصميم **Minimal** (ألوان محايدة، بدون Logo نهائي) قابل للاستبدال لاحقًا دون إعادة هيكلة الكود.
4. **الدفع/الاشتراكات (بعد المرحلة 6):** تقرّر نهائيًا عدم استخدام أي بوابة دفع خارجية (لا Stripe لصعوبة التحقق بدون رقم أمريكي، ولا Paddle لتفادي أي اعتماد خارجي) — المنتج **مجاني بالكامل** إلى أجل غير مسمى. المرحلة 9 أُلغيت وحُذفت كل حقول بوابة الدفع من الـ Schema.
5. **التحوّل للاستقلالية (بعد المرحلة 8):** أُلغيت **كل** الخدمات الخارجية بقرار المالك واستُبدلت بحلول مستقلة: Supabase Postgres → **SQLite**، Supabase Storage → **قرص محلي**، Upstash Redis + BullMQ → **worker يستطلع SQLite**، Clerk → **مصادقة مبنيّة ذاتيًا** (`node:crypto`). المتبقي الخارجي الوحيد: **OpenAI Whisper**. النتيجة: المشروع يعمل محليًا بمفتاح OpenAI فقط. (المراحل 3، 5، 6 أُعيد بناؤها؛ كلها مُختبرة end-to-end بالستاك الجديد.)

---

## المرحلة 0 — تحضير الحسابات والمفاتيح 🔴 ✅ (لم تعد لازمة — استقلالية كاملة)

> بعد التحوّل للاستقلالية (القرار #5)، الحسابات الخارجية ما عادت مطلوبة.
- [x] **OpenAI** — المفتاح الوحيد المطلوب (`OPENAI_API_KEY`).
- [x] ~~Clerk~~ → مصادقة ذاتية. ~~Supabase~~ → SQLite + قرص محلي. ~~Upstash~~ → polling. ~~Stripe~~ → ملغى.
- [ ] التأكد من وجود `OPENAI_API_KEY` صالح مع صلاحيات كافية على Whisper API.
- [ ] توثيق كل المفاتيح في ملف `.env.example` (بدون قيم حقيقية) على مستوى الـ Monorepo.

> بعد إنجاز هذه المرحلة، ننتقل مباشرة إلى **المرحلة 1** (الإصلاحات العاجلة) ثم **المرحلة 2** (البنية المعمارية) بالترتيب الموجود أعلاه.
