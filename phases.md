# خطة تطوير المشروع: Audio-to-Text SaaS

> **الوضع الحالي:** سكريبت Python واحد (`main.py`) يستخدم OpenAI Whisper API لتحويل ملف صوتي محدد يدويًا إلى نص. لا يوجد Frontend، لا يوجد Backend حقيقي، لا يوجد Auth، لا توجد قاعدة بيانات، لا يوجد نظام دفع.
>
> **الهدف:** تحويل المشروع إلى منتج SaaS حقيقي جاهز لاستقبال مستخدمين فعليين، مع صفحة تسويقية (Promo Page)، تسجيل دخول/اشتراك، Dashboard لرفع الملفات الصوتية وتحويلها لنص، نظام اشتراكات/حدود استخدام، وبنية تحتية قابلة للتوسّع.

## القرارات التقنية المعتمدة

| القرار | الاختيار | السبب |
|---|---|---|
| Frontend | **Next.js (React) + TypeScript** | SEO أفضل للـ Promo Page، Server Components، سهولة النشر على Vercel |
| Backend | **Next.js API Routes** (للعمليات السريعة) + **خدمة Node/Express منفصلة (Worker)** للمعالجة الطويلة | تفادي حدود وقت التنفيذ في Serverless عند تحويل ملفات صوتية طويلة |
| Auth | **Clerk** | تكامل جاهز مع Next.js، يدعم OAuth (Google)، يوفر UI جاهز لصفحات Login/Sign Up |
| قاعدة البيانات | **PostgreSQL (Supabase أو Neon)** + **Prisma ORM** | مُدارة، منخفضة الصيانة، تدعم Row-Level Security |
| تخزين الملفات | **Supabase Storage أو AWS S3** | لا نخزن الصوت على السيرفر مباشرة، روابط رفع مباشرة (Signed URLs) |
| قائمة الانتظار (Queue) | **BullMQ + Redis (Upstash)** | لمعالجة التحويلات الطويلة بشكل غير متزامن (Async) |
| الدفع/الاشتراكات | **Stripe** | خطط Free / Pro، فوترة حسب الاستخدام (دقائق صوت) |
| المراقبة | **Sentry (أخطاء) + PostHog/Plausible (تحليلات)** | معرفة الأعطال والاستخدام الفعلي |
| الاستضافة | **Vercel (Frontend + API Routes)** + **Railway/Render/Fly.io (Worker + Redis)** | فصل الأحمال الخفيفة عن الثقيلة |

---

## نظرة عامة على المراحل (مرتّبة حسب الأهمية)

| # | المرحلة | الأولوية |
|---|---|---|
| 1 | إصلاحات أمنية وبنيوية عاجلة على الكود الحالي | 🔴 حرجة |
| 2 | تصميم البنية المعمارية والـ Monorepo | 🔴 حرجة |
| 3 | قاعدة البيانات ونماذج البيانات (Schema) | 🔴 حرجة |
| 4 | Backend API الأساسي (رفع + تحويل الصوت) | 🔴 حرجة |
| 5 | نظام تسجيل الدخول (Auth: Login / Sign Up) | 🔴 حرجة |
| 6 | معالجة غير متزامنة (Queue + Worker) لدعم الملفات الطويلة | 🟠 عالية |
| 7 | Frontend - لوحة التحكم (Dashboard) الأساسية | 🟠 عالية |
| 8 | صفحة تسويقية (Promo / Landing Page) | 🟠 عالية |
| 9 | نظام الاشتراكات والدفع (Stripe) + حدود الاستخدام | 🟠 عالية |
| 10 | تخزين الملفات السحابي (Cloud Storage) | 🟡 متوسطة |
| 11 | الأمان المتقدّم وحماية من إساءة الاستخدام | 🟡 متوسطة |
| 12 | المراقبة والتحليلات (Observability) | 🟡 متوسطة |
| 13 | الاختبارات (Testing) | 🟡 متوسطة |
| 14 | CI/CD والنشر | 🟡 متوسطة |
| 15 | الامتثال القانوني (Privacy Policy / ToS / GDPR) | 🟢 منخفضة (لكن مطلوبة قبل الإطلاق الفعلي) |
| 16 | تحسينات ما بعد الإطلاق | 🟢 منخفضة |

---

## المرحلة 1 — إصلاحات عاجلة على الكود الحالي 🔴 ✅ (مُنجزة)

هذه مشاكل موجودة في `main.py` الحالي ويجب حلها فورًا حتى لو بشكل مؤقت قبل إعادة البناء الكامل:

- [ ] إصلاح الخطأ الإملائي `clint` → `client`.
- [ ] إزالة المسار الوهمي `"your mp3 file"` — يجب أن يأتي من مدخل حقيقي (CLI arg أو API request) وليس Hardcoded.
- [ ] استخدام `with open(...)` بدلاً من `open()` بدون إغلاق الملف (تسريب Resource).
- [ ] عدم رفع مفتاح `OPENAI_API_KEY` أو ملف `.env` للـ Git — التأكد من وجود `.gitignore` يشمل `.env` و`.venv`.
- [ ] إضافة معالجة أخطاء أساسية (ملف غير موجود، امتداد غير مدعوم، فشل الاتصال بـ API).
- [ ] إضافة `requirements.txt` أو الانتقال المباشر لبنية Node.js الجديدة.

**ملاحظة:** هذه المرحلة يمكن تنفيذها خلال ساعة، لكنها ضرورية كنقطة انطلاق نظيفة قبل الهدم/البناء.

---

## المرحلة 2 — البنية المعمارية و Monorepo 🔴 ✅ (مُنجزة)

- [ ] إنشاء بنية Monorepo (باستخدام `pnpm workspaces` أو `Turborepo`):
  ```
  /apps
    /web        → Next.js (Frontend + API Routes الخفيفة)
    /worker     → Node/Express + BullMQ (معالجة التحويل الصوتي)
  /packages
    /db         → Prisma schema + Client مشترك
    /shared     → أنواع TypeScript مشتركة (types/DTOs)
  ```
- [ ] إعداد متغيرات البيئة (`.env`) لكل تطبيق على حدة مع `zod` للتحقق من صحتها عند الإقلاع.
- [ ] إعداد ESLint + Prettier + TypeScript strict mode موحّد على مستوى الـ Monorepo.
- [ ] توثيق البنية في `README.md` جديد للمشروع.

---

## المرحلة 3 — قاعدة البيانات ونماذج البيانات 🔴 ✅ (مُنجزة — schema حيّ على Supabase)

تصميم Schema أولي عبر Prisma:

- `User` (مرتبط بـ Clerk عبر `clerkId`)
- `Subscription` (خطة المستخدم: Free / Pro، حالة Stripe)
- `Transcription` (ملف صوتي، حالة المعالجة: pending/processing/done/failed، النص الناتج، المدة الزمنية للصوت، التكلفة)
- `UsageLog` (لتتبع الدقائق المستهلكة شهريًا لكل مستخدم لأغراض الفوترة والحدود)

- [ ] كتابة الـ Schema وتوليد الـ Migrations.
- [ ] إعداد Seed script للبيانات التجريبية.
- [ ] تفعيل Row-Level Security إن استُخدم Supabase مباشرة.

---

## المرحلة 4 — Backend API الأساسي 🔴

- [ ] Endpoint: `POST /api/transcriptions` — استقبال ملف صوتي (أو رابط Signed Upload)، التحقق من الحجم/الامتداد/مدة الصوت.
- [ ] Endpoint: `GET /api/transcriptions/:id` — حالة ونتيجة التحويل.
- [ ] Endpoint: `GET /api/transcriptions` — سجل تحويلات المستخدم.
- [ ] دمج استدعاء Whisper API (نقل منطق `main.py` الحالي إلى خدمة Node منظّمة، أو الإبقاء عليه كـ Python microservice إن أردت إعادة استخدام الكود — **قرار مفتوح، انظر الأسئلة بالأسفل**).
- [ ] التحقق من صحة المدخلات (حجم أقصى، صيغ مدعومة: mp3/wav/m4a...).
- [ ] معالجة الأخطاء بشكل موحّد (Error format ثابت لكل الـ API).

---

## المرحلة 5 — تسجيل الدخول (Auth) 🔴

- [ ] تكامل Clerk مع Next.js (`@clerk/nextjs`).
- [ ] صفحات `/sign-in` و `/sign-up` باستخدام مكوّنات Clerk الجاهزة مع تخصيص الشكل ليطابق هوية المنتج.
- [ ] حماية مسارات Dashboard عبر Middleware.
- [ ] Webhook من Clerk لإنشاء سجل `User` في قاعدة البيانات تلقائيًا عند التسجيل.
- [ ] دعم تسجيل الدخول عبر Google (OAuth) كخيار سريع.

---

## المرحلة 6 — المعالجة غير المتزامنة (Queue + Worker) 🟠

- [ ] إعداد Redis (Upstash) + BullMQ.
- [ ] عند رفع ملف صوتي: إنشاء Job في القائمة بدل انتظار الاستجابة مباشرة.
- [ ] الـ Worker (تطبيق Node منفصل) يسحب المهام، يستدعي Whisper API، يحدّث حالة `Transcription` في القاعدة.
- [ ] دعم تقسيم الملفات الصوتية الطويلة (Chunking) إذا تجاوزت حد Whisper (25MB).
- [ ] إشعار الواجهة الأمامية بانتهاء المعالجة (Polling أو WebSocket/SSE).

---

## المرحلة 7 — Frontend: لوحة التحكم (Dashboard) 🟠

- [ ] صفحة رفع الملفات (Drag & Drop) مع شريط تقدّم.
- [ ] عرض حالة المعالجة لحظيًا (Processing/Done/Failed).
- [ ] عرض النص الناتج مع خيارات: نسخ، تحميل (TXT/SRT/VTT)، بحث داخل النص.
- [ ] سجل كامل لعمليات التحويل السابقة مع بحث وفلترة.
- [ ] عرض الرصيد المتبقي من الدقائق حسب خطة الاشتراك.
- [ ] تصميم متجاوب (Responsive) لكل الشاشات.

---

## المرحلة 8 — الصفحة التسويقية (Promo / Landing Page) 🟠

- [ ] Hero Section: عرض واضح للقيمة المقدّمة (تحويل صوت لنص بدقة عالية وبسرعة).
- [ ] قسم المزايا (Features) مع أيقونات/رسومات.
- [ ] قسم الأسعار (Pricing) يعرض خطط Free/Pro بوضوح.
- [ ] قسم "كيف يعمل" (How it works) بخطوات بسيطة.
- [ ] Testimonials / Social proof (حتى لو placeholder مبدئيًا).
- [ ] CTA واضح يقود لصفحة Sign Up.
- [ ] تحسين SEO أساسي (meta tags, Open Graph, sitemap.xml, robots.txt).
- [ ] سرعة تحميل عالية (Lighthouse score) لأن هذه الصفحة أول تفاعل مع المستخدم المحتمل.

---

## المرحلة 9 — الاشتراكات والدفع (Stripe) + حدود الاستخدام 🟠

- [ ] تكامل Stripe Checkout + Customer Portal.
- [ ] خطة Free: عدد دقائق شهري محدود (مثلاً 30 دقيقة).
- [ ] خطة Pro: دقائق أكبر أو غير محدودة + ميزات إضافية (تصدير SRT، دعم أولوية).
- [ ] Webhook من Stripe لتحديث حالة الاشتراك في القاعدة فورًا.
- [ ] منطق فرض الحدود (Quota enforcement) قبل قبول أي Job جديد.
- [ ] صفحة "Billing" داخل الحساب لعرض الفواتير وتغيير الخطة.

---

## المرحلة 10 — تخزين الملفات السحابي 🟡

- [ ] رفع الملفات مباشرة من المتصفح إلى S3/Supabase Storage عبر Signed URL (بدون المرور بالسيرفر لتقليل الحمل).
- [ ] سياسة حذف تلقائي للملفات الصوتية بعد فترة معينة (مثلاً 7 أيام) لتقليل التكلفة وحماية الخصوصية.
- [ ] الاحتفاظ بالنص الناتج فقط على المدى الطويل، وليس الملف الصوتي نفسه.

---

## المرحلة 11 — الأمان وحماية من إساءة الاستخدام 🟡

- [ ] Rate limiting على مستوى الـ API (per-user و per-IP) لمنع استنزاف تكلفة Whisper API.
- [ ] حد أقصى لحجم الملف ومدة الصوت لكل طلب.
- [ ] فحص نوع الملف الحقيقي (Magic bytes) وليس فقط الامتداد، لمنع رفع ملفات ضارة.
- [ ] CORS محكم على الـ API.
- [ ] تدوير المفاتيح السرّية (API keys) وتخزينها في Secret Manager للاستضافة (وليس `.env` في الإنتاج).
- [ ] حماية الـ Webhooks (Stripe/Clerk) بالتحقق من التوقيع (Signature verification).

---

## المرحلة 12 — المراقبة والتحليلات 🟡

- [ ] Sentry لتتبع الأخطاء في Frontend و Backend و Worker.
- [ ] Logging منظّم (structured logs) في الـ Worker لتتبع مدة كل Job وتكلفته.
- [ ] لوحة تحليلات استخدام بسيطة (PostHog) لمعرفة معدل التحويل من الزائر إلى مستخدم مسجّل، ومن Free إلى Pro.
- [ ] تنبيهات (Alerts) عند ارتفاع معدل فشل الـ Jobs أو ارتفاع غير طبيعي في تكلفة Whisper API.

---

## المرحلة 13 — الاختبارات 🟡

- [ ] اختبارات وحدة (Unit tests) لمنطق التحقق من المدخلات وحدود الاستخدام.
- [ ] اختبارات تكامل (Integration) لمسار: رفع → معالجة → نتيجة.
- [ ] اختبارات E2E (Playwright) للمسارات الحرجة: Sign up → رفع ملف → استلام نص → الترقية لخطة مدفوعة.
- [ ] اختبار تحميل (Load test) بسيط على الـ Worker للتأكد من التعامل مع طلبات متزامنة.

---

## المرحلة 14 — CI/CD والنشر 🟡

- [ ] GitHub Actions: Lint + Type-check + Tests على كل Pull Request.
- [ ] نشر تلقائي لـ `apps/web` على Vercel عند الدمج بـ `main`.
- [ ] نشر تلقائي لـ `apps/worker` على Railway/Render/Fly.io.
- [ ] بيئات منفصلة: Development / Staging / Production مع متغيرات بيئة مستقلة.

---

## المرحلة 15 — الامتثال القانوني 🟢

- [ ] صفحة Privacy Policy توضح كيفية التعامل مع الملفات الصوتية المرفوعة.
- [ ] صفحة Terms of Service.
- [ ] موافقة صريحة من المستخدم عند الرفع (خاصة إن كانت الملفات تحتوي بيانات حساسة/محادثات).
- [ ] التأكد من امتثال OpenAI نفسه لسياسات البيانات (مراجعة اتفاقية OpenAI API Data Usage).

---

## المرحلة 16 — تحسينات ما بعد الإطلاق 🟢

- [ ] دعم لغات متعددة في الواجهة (i18n) — خاصة العربية والإنجليزية.
- [ ] تصدير بصيغ ترجمة (SRT/VTT) مع ضبط التوقيت (timestamps).
- [ ] دعم تمييز المتحدثين (Speaker diarization) كخاصية Pro.
- [ ] واجهة برمجية (API) عامة للمطورين ليستخدموا الخدمة برمجيًا (خطة B2B).
- [ ] دعم Workspaces/Teams لمشاركة التحويلات بين أعضاء فريق.
- [ ] تطبيق جوّال (React Native) في مرحلة لاحقة إن نجح المنتج.

---

## القرارات النهائية (تم الحسم)

1. **لغة منطق التحويل الصوتي:** إعادة كتابة منطق Whisper بالكامل بـ **Node.js** — لا نُبقي على Python، الـ Stack كله JavaScript/TypeScript موحّد (Next.js + Worker).
2. **الحسابات الخارجية (Stripe/Clerk/Supabase):** غير جاهزة بعد → أُضيفت **مرحلة 0** أدناه لإنشاء وربط هذه الحسابات قبل البدء بالتطوير الفعلي.
3. **الهوية البصرية:** غير جاهزة → صفحة الـ Promo تبدأ بتصميم **Minimal** (ألوان محايدة، بدون Logo نهائي) قابل للاستبدال لاحقًا دون إعادة هيكلة الكود.

---

## المرحلة 0 — تحضير الحسابات والمفاتيح (قبل أي كود) 🔴 ✅ (مُنجزة — عدا Stripe المؤجّل)

- [ ] إنشاء حساب **Clerk** + مشروع جديد + الحصول على `CLERK_PUBLISHABLE_KEY` و `CLERK_SECRET_KEY`.
- [ ] إنشاء حساب **Supabase** (أو Neon) + إنشاء مشروع قاعدة بيانات + الحصول على `DATABASE_URL`.
- [ ] إنشاء حساب **Stripe** (وضع Test Mode أولاً) + إنشاء المنتجات/الخطط (Free/Pro) + الحصول على `STRIPE_SECRET_KEY` و `STRIPE_WEBHOOK_SECRET`.
- [ ] إنشاء حساب **Upstash Redis** (لـ BullMQ) + الحصول على `REDIS_URL`.
- [ ] التأكد من وجود `OPENAI_API_KEY` صالح مع صلاحيات كافية على Whisper API.
- [ ] توثيق كل المفاتيح في ملف `.env.example` (بدون قيم حقيقية) على مستوى الـ Monorepo.

> بعد إنجاز هذه المرحلة، ننتقل مباشرة إلى **المرحلة 1** (الإصلاحات العاجلة) ثم **المرحلة 2** (البنية المعمارية) بالترتيب الموجود أعلاه.
